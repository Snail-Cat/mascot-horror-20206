I drew this as soon as I got Batata… I was so happy and used up almost all my glitter.
if GlobalState.get_flag("DINNERROOM_GLITTER_GET") == false:
	You found some glitter.
	do GlobalState.add_inventory_item("GLITTER")
	do GlobalState.set_flag("DINNERROOM_GLITTER_GET")